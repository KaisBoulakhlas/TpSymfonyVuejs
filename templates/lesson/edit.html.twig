{% extends 'base.html.twig' %}

{% block title %}Editer une leçon{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>Editer une leçon</h1>
        <hr/>
        <div class="container">
            {{ form_start(form) }}
            <div class="form-row">
                <div class="form-group col-md-4">
                    {{ form_row(form.title, {'attr': {'class': 'form-control'}} ) }}
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-md-4">
                    {{ form_row(form.teacher, {'attr': {'class': 'form-control'}} ) }}
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-md-4">
                    {{ form_row(form.startAt, {'attr': {'class': 'form-control'}} ) }}
                </div>
            </div>
            <div class="form-row">
                <div class="form-group col-md-4">
                    {{ form_row(form.endAt, {'attr': {'class': 'form-control'}} ) }}
                </div>
            </div>
            <div class="form-row">
                {{ form_widget(form)}}
                    <button class="btn btn-outline-warning" style="margin-left:5px;">{{ button|default('Editer')}}</button>
                {{ form_end(form)}}
            </div>
        </div>
    </div>
{% endblock %}